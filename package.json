{
  "name": "slippys",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
      "build": "pnpm run -r build"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "esbuild": "^0.24.0"
  }
}
